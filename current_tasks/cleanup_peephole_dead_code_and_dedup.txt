Task: Clean up x86 peephole optimizer dead code and duplicated logic

The recent merge of 5 peephole local passes into combined_local_pass left behind
~240 lines of dead code (functions with #[allow(dead_code)]). Additionally:
- Redundant extension checks are duplicated within combined_local_pass
- The cltq check at lines 539-549 is a subset of the later is_redundant_ext block
- parse_store_to_rbp_str and parse_load_from_rbp_str duplicate mov prefix parsing
- "clear all mappings" pattern repeated 4 times in global_store_forwarding

Changes:
1. Delete 5 dead-code functions (~240 lines)
2. Remove redundant cltq check in combined_local_pass
3. Extract shared strip_mov_prefix helper
4. Extract clear_all_mappings helper
5. Clean up and add documentation
